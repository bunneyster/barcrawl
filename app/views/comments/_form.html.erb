<%= form_for tour_stop.new_comment do |f| %>
  <%= f.hidden_field :tour_stop_id %>
  
  <%= f.text_area :text, class: 'enter-submits-form',
          placeholder: 'Add your own comment!',
          autofocus: true, required: true %>
<% end %>
